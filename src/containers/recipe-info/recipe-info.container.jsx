import React from "react";


// It contains info about laden recipes.
//
class recipeInfo extends React.Component {
  render() {
    return (
      <h2>That's RecipeInfo container</h2>
    );
  };
};

export default recipeInfo;
