import styled from "styled-components";
import Logo from '../../logo/logo.component';

export const TopNavBarConainer = styled.header`
  margin: 0;
  padding: 0;
  list-style: none;
  display: flex;
  flex-flow: row;
  align-items: right;
  height: 100%;
`
export const LogoElement = styled(Logo)`
  height: 80%;
`
